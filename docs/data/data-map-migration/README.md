# Data map migration

Reports on moving the data map from `datamap.csv` to code (pull request #372). They are self-contained HTML pages:
open them in a browser from a local clone (double-click, or `start report.html`). GitHub shows the source, not the page.

- [report.html](report.html) – errors fixed, platform improvements and follow-ups
- [renamed-labels-demo.html](renamed-labels-demo.html) – walk-through of the school EAL and not-disadvantaged figures that went blank after DfE renamed the labels
