@using SAPSec.Web.Areas.Shared.ViewModels.Comparison
@using SAPSec.Web.Constants
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData[ViewDataKeys.SkipLinkTargetId] = "page-content";

    var layout = ViewData[ViewDataKeys.ComparisonLayout] as ComparisonLayoutModel;
    ViewData[ViewDataKeys.Title] = layout!.ComparatorSchool.Name;

    var currentAction = (string?)ViewContext.RouteData.Values["action"];
    string active(ComparisonNavigationItem item) => item.ActiveActionNames.Contains(currentAction ?? string.Empty)
        ? "govuk-service-navigation__item--active"
        : "";
}

@section Breadcrumbs {
    <nav class="govuk-breadcrumbs govuk-breadcrumbs--collapse-on-mobile school-comparison-breadcrumb" aria-label="Breadcrumb">
        <ol class="govuk-breadcrumbs__list">
            <li class="govuk-breadcrumbs__list-item">
                <a class="govuk-breadcrumbs__link" href="@Routes.FindASchool()">Home</a>
            </li>
            <li class="govuk-breadcrumbs__list-item">
                <a class="govuk-breadcrumbs__link"
                   href="@layout.ViewSimilarSchoolsUrl">
                    View similar schools
                </a>
            </li>
        </ol>
    </nav>
}

<div class="govuk-grid-row app-comparison-pages">

    @* LEFT: back link *@
    <div class="app-side-navigation-container">
        <a class="govuk-back-link"
           href="@layout.ViewSimilarSchoolsUrl">
            Back
        </a>
    </div>

    @* MAIN CONTENT *@
    <div class="app-body-container-with-side-navigation" id="page-content" tabindex="-1">
        <span class="govuk-caption-xl">
            @layout.CurrentSchool.Name
        </span>

        <span
            class="govuk-caption-m govuk-!-font-size-24 govuk-!-font-weight-bold govuk-!-padding-top-1 govuk-!-padding-bottom-1">
                Comparing to
            </span>

        <h1 class="govuk-heading-xl">
            @ViewData[ViewDataKeys.Title]
        </h1>

        <div class="govuk-service-navigation compare-nav">
            <div class="govuk-service-navigation__container">
                <nav aria-label="Compare sections" class="govuk-service-navigation__wrapper">
                    <ul class="govuk-service-navigation__list">

                        @foreach (var item in layout.NavigationItems)
                        {
                            var isCurrent = item.ActiveActionNames.Contains(currentAction ?? string.Empty);
                            <li class="govuk-service-navigation__item @active(item)">
                                <a class="govuk-service-navigation__link"
                                   href="@item.Href"
                                   aria-current="@(isCurrent ? "page" : null)">
                                    @item.Text
                                </a>
                            </li>
                        }

                    </ul>
                </nav>
            </div>
        </div>

        <div class="govuk-!-margin-top-9">
            @RenderBody()
        </div>

    </div>
</div>


@await RenderSectionAsync("Scripts", required: false)
