@using System.Globalization
@using System.Text.Encodings.Web
@using System.Text.Json
@model SAPSec.Web.ViewModels.SchoolSearchResultViewModel[]

@{
    var inv = CultureInfo.InvariantCulture;
    
    var schools = Model
        .Select(s =>
        {
            var okLat = double.TryParse(s.Latitude, NumberStyles.Float, inv, out var lat);
            var okLon = double.TryParse(s.Longitude, NumberStyles.Float, inv, out var lon);

            if (!okLat || !okLon) return null;

            return new
            {
                urn = s.URN,
                name = s.SchoolName,
                address = s.Address,
                la = s.LocalAuthority,
                lat,
                lon,
                url = Url.Action("Index", "School", new { urn = s.URN })
            };
        })
        .Where(x => x != null)
        .ToList();

    var json = JsonSerializer.Serialize(schools, new JsonSerializerOptions
    {
        Encoder = JavaScriptEncoder.UnsafeRelaxedJsonEscaping
    });
}

<div class="results-bar govuk-!-margin-bottom-2">
    <p class="govuk-body govuk-!-margin-bottom-0" id="mapCount">
        Showing <strong>@Model.Length</strong> schools
    </p>
    
    <div id="mapBarActions"></div>
</div>
<div class="govuk-clearfix">

    <div id="map"
         data-fixed-zoom="14"
         role="region"
         aria-label="Map of schools">
        <div class="map-loading">Loading map…</div>
    </div>


</div>

<script type="application/json" id="schools-data">
@Html.Raw(json)
</script>